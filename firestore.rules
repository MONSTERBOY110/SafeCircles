rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Dev rules: any authenticated user can read/write.
    // TODO: tighten before production (per-collection ownership checks).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}